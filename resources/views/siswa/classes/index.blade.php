{{-- siswa/classes/index.blade.php — FR-SW-03 / FR-SW-04 / BR-01 / NFR-08 / M3 --}}
@extends('layouts.app')
@section('breadcrumb')<x-breadcrumb :items="[['label' => 'Dashboard', 'url' => route('siswa.dashboard')], ['label' => 'Kelas Saya']]" />@endsection
@section('content')
<div class="grid gap-5 lg:gap-7.5"><div><h1 class="text-xl font-medium text-mono">Kelas Saya</h1><p class="text-sm text-secondary-foreground">Gabung kelas menggunakan kode.</p></div>@if(session('success'))<div class="kt-alert kt-alert-success">{{ session('success') }}</div>@endif<div class="kt-card"><form method="POST" action="{{ route('siswa.classes.join') }}">@csrf<div class="kt-card-content flex flex-wrap gap-3 p-5"><input name="class_code" class="kt-input grow @error('class_code') border-destructive @enderror" placeholder="Kode kelas" value="{{ old('class_code') }}" required /><button class="kt-btn kt-btn-primary">Gabung Kelas</button></div>@error('class_code')<p class="text-destructive text-xs px-5 pb-5">{{ $message }}</p>@enderror</form></div><div class="grid md:grid-cols-2 xl:grid-cols-3 gap-5">@forelse($classes as $class)<a class="kt-card hover:border-primary" href="{{ route('siswa.classes.show', $class) }}"><div class="kt-card-content grid gap-2 p-5"><span class="text-base font-medium">{{ $class->name }}</span><span class="text-sm text-secondary-foreground">Guru: {{ $class->guru->name }}</span><span class="kt-badge kt-badge-outline self-start">{{ $class->is_active ? 'Aktif' : 'Nonaktif' }}</span></div></a>@empty<div class="text-secondary-foreground">Belum bergabung ke kelas.</div>@endforelse</div></div>
@endsection
